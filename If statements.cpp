#include <iostream>
using namespace std;

int main(){
    int x = 3;
    int y = 2;
    int z = 1;
    
    if (x>=y){
              cout << x <<" is greter than or equal to " << y << endl; }
    if (y>=x){
              cout <<y <<" is greter than or equal to " << x << endl; }
    if (z>=y){
              cout <<z <<" is greter than or equal to " << y << endl; }
              
              
    
    system("pause");
    return 0;
}
